# Core dependencies for the Smart Classroom Attendance System
opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
Pillow>=9.5.0

# Advanced Face Recognition Library (depends on dlib and CMake)
# Note: On Windows, you need Visual Studio C++ Build Tools and CMake installed to compile dlib.
# If you run into issues installing this, the project has an automatic OpenCV fallback.
face-recognition>=1.3.0
